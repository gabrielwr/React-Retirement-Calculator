import styled from "styled-components";

export const AgeFormWrapper = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 7%;
`;

export const AgeFormInput = styled.input`
  border: 0;
  outline: 0;
  background: transparent;
  border-bottom: 1px dashed black;
  text-align: center;
  margin: 0 10px 0 10px;
`;
